// This key is used in the headers of a request to determine whether or not we should
// intercept the request and add the auth credentials to it.
export const includeAuthHeaderKey: string = 'x-include-auth';
