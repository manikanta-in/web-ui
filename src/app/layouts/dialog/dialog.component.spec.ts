describe('DialogComponent', () => {
    // This component is tested as a part of the
    // dialog service instead; as the service manages
    // DOM operations and showing/destroying dialog
    // instances.
});
